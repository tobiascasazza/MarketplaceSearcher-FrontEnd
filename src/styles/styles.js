import { lightBlue } from "@mui/material/colors";
import { makeStyles } from "@mui/styles";
import { borderColor } from "@mui/system";

const useStyles = makeStyles((theme) => ({
    barraSuperior:{
        backgroundColor: '#1a74d0'
    },
    searcher:{
        color: "white",
        

        
    },
    lupa: {
        color: 'white',
        cursor: "pointer"
    },
    noBoton:{
        cursor: "pointer"
    }
}));

export default useStyles;
